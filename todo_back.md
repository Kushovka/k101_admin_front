- Нужно сделать на бэке новую ручку для создания пользователей через фронтенд. POST-эндпоинт, например /admin/users/create, который принимает JSON с полями, как у всех пользователей в таблице. Доступ к ручке только для админов, ручка должна отображаться в Swagger/OpenAPI.

- Сделать новую ручку пример upload/status/${task_id} что бы видеть статус загруженного файла

- сделать ручку для новой страницы тарифов

  GET /plans — получить все доступные тарифы

  POST /plans — создать тариф (админ)
  (цена тарифа (price), время действия плана в месяцах(month), название плана(plan_name))

  PUT /plans/:id — редактировать тариф (админ)
  (изменяю цену тарифа (price), время действия плана в месяцах(month), название плана(plan_name))

  DELETE /plans/:id — удалить тариф (админ)
